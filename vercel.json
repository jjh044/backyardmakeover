{
  "framework": "vite",
  "installCommand": "npm install",
  "buildCommand": "mkdir -p .convex-tmp && CONVEX_TMPDIR=$PWD/.convex-tmp npx convex deploy --cmd-url-env-var-name VITE_CONVEX_URL --cmd \"npm run build\"",
  "outputDirectory": "dist"
}
